import { CheckCircle2, ShieldCheck } from "lucide-react";
import type { PolicyEvent } from "../types";
import { Badge, EmptyState, Panel, SectionHeader } from "./ui";

export function PolicyPanel({ events, embedded = false }: { events: PolicyEvent[]; embedded?: boolean }) {
  const content = events.length === 0 ? <EmptyState title="No policy evaluation emitted" description="This run did not emit a policy audit event in the current projection." icon={ShieldCheck} /> : <div className="space-y-3">{events.map((event, index) => <div className="decision-block" key={`${event.tool_name}-${index}`}><div className="flex items-start justify-between gap-3"><div><div className="eyebrow">Policy evaluation</div><div className="mt-1 text-sm font-semibold capitalize text-main">{event.outcome.replace(/_/g, " ")}</div><div className="mt-1 font-mono text-[11px] text-muted">{event.tool_name}</div></div><Badge tone={event.outcome === "allow" ? "success" : event.outcome === "deny" ? "danger" : "warning"}>Risk {event.risk_level}</Badge></div><div className="policy-detail-grid mt-4"><div><div className="field-label">Confirmation</div><div className="mt-1 text-xs capitalize text-main">{event.confirmation_status?.replace(/_/g, " ") ?? "Not recorded"}</div></div><div><div className="field-label">Execution</div><div className="mt-1 text-xs capitalize text-main">{event.execution_status?.replace(/_/g, " ") ?? "Not recorded"}</div></div></div><div className="mt-4 space-y-2">{event.reason_codes.length ? event.reason_codes.slice(0, 4).map((code) => <div className="flex items-center gap-2 text-xs capitalize text-muted" key={code}><CheckCircle2 size={14} className="text-success" aria-hidden="true" />{code.replace(/_/g, " ")}</div>) : <div className="text-xs text-muted">No policy reason codes recorded.</div>}</div></div>)}</div>;
  return embedded ? <div>{content}</div> : <Panel title="Policy evaluation" eyebrow="Authorization boundary"><SectionHeader title="Deterministic policy" description="Structured outcomes and validation checkpoints." />{content}</Panel>;
}
